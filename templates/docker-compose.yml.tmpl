# Generated by swiftdeploy — do not edit directly
# Source: templates/docker-compose.yml.tmpl

services:

  nginx:
    image: NGINX_IMAGE
    container_name: nginx_proxy
    ports:
      - "NGINX_PORT:NGINX_PORT"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - nginx_logs:/var/log/nginx
    networks:
      - NETWORK_NAME
    depends_on:
      service_backend:
        condition: service_healthy
    restart: unless-stopped

  service_backend:
    image: SERVICE_IMAGE
    container_name: service_backend
    expose:
      - "SERVICE_PORT"
    environment:
      - MODE=SERVICE_MODE
      - VERSION=SERVICE_VERSION
      - PORT=SERVICE_PORT
    networks:
      - NETWORK_NAME
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - service_logs:/app/logs
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:SERVICE_PORT/healthz || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    restart: unless-stopped

networks:
  NETWORK_NAME:
    driver: NETWORK_DRIVER

volumes:
  nginx_logs:
  service_logs: